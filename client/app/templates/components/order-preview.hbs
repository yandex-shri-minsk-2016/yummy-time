<a href="/orders/join" class="order-preview">
  <div class="order-preview__row">
    <div class="order-preview__place">{{order.place}}</div>
    <time class="order-preview__time">{{order.time}}</time>
  </div>
  <div class="order-preview__row">
    <div class="order-preview__manager">{{order.manager}}</div>
    <div class="order-preview__amount">{{order.amount}}</div>
  </div>
</a>
