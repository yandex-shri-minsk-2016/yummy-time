{{#link-to 'new-portion-order' order class="f-preview"}}
  <div class="f-preview__row">
    <div class="f-preview__place">{{order.vendor.title}}/{{order.location}}</div>
    <time class="f-preview__time">{{order.time}}</time>
  </div>
  <div class="f-preview__row">
    <div class="f-preview__manager">{{order.manager}}</div>
  </div>
{{/link-to}}
